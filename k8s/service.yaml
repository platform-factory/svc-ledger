apiVersion: v1
kind: Service
metadata:
  name: svc-ledger
  namespace: svc-ledger
  labels:
    app.kubernetes.io/name: svc-ledger
spec:
  selector:
    app.kubernetes.io/name: svc-ledger
  ports:
    - name: http
      port: 80
      targetPort: http
